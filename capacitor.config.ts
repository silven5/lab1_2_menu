import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'ionic.lab1_2_menu',
  appName: 'lab1_2_menu',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
